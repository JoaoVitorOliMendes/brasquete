export interface ClassColor {
    primary?: string,
    secondary?: string,
    white?: string,
    black?: string
}

export interface ClassTypeColor {
    outline?: ClassColor,
    filled?: ClassColor
}